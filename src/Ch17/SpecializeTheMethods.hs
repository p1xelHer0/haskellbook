module SpecializeTheMethods where

-- 1.
-- []

-- methods
-- pure :: [(a -> a)] -> [a] -> [b]


-- 2.
-- IO

-- methods
-- pure :: ? (a -> b) -> ? a -> ? b

-- 3.
-- (,) a
-- pure :: ? (a -> b) -> ? a -> ? b
